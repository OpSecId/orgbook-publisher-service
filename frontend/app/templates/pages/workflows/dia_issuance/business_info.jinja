{% extends('layouts/base.min.jinja') %}
{% block page %}
<div class="container-tight">
    {% include('components/brand-header.jinja') %}
    <form method="post">
        <div class="card">
            <div class="card-body">
                <div class="row align-items-center">
                    {# <div class="col-4">
                        <img src="https://avatars.githubusercontent.com/u/916280"
                            alt="Business information" class="rounded">
                    </div> #}
                    <div class="col">
                        <h3 class="card-title mb-1">
                            <div class="text-reset">{{ credential['credentialSubject']['name'] }}</div>
                        </h3>
                        <div class="text-secondary">
                            {{ credential['credentialSubject']['registerType'] }} {{ credential['credentialSubject']['registeredId'] }}
                        </div>
                        <div class="mt-3">
                            <a href="{{ url_for('main.dia_issuance_did_auth') }}" class="btn btn-sm btn-outline-dark w-100">Confirm</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </form>
</div>
{% endblock %}